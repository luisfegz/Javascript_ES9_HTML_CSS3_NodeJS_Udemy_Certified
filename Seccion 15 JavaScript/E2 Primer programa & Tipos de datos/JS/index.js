console.log('Hola Mundo :D');
/**
 * ? HOLA Better Coments
 * ! QUE 
 * * bueno
 * TODO: xd xd
 */

/**
 * TODO // Tipos de datos en JS
 * ! String: cadena de caracteres. 'a' 'hola' 'hola mundo'
 * * Boolean: true false
 * ? Null: Variable vacia / Nulo
 * Number: Cualquier numero 123456789
 * "123" es un string, es distinto a 123 que es un number
   Undefined: No se encuentra una variable definida
* * Object: Objeto 
*/


// * Definicion de Variables
// ? var 
// ! let
// todo: const

//var miPrimeraVariable = 'lala';

// Se ejecuta entrando a la direccion del archivo y luego: node index.js

let miPrimerVariable = 'Mi primera variable';
console.log(miPrimerVariable);

// Cambios de la variable
// * Mutabilidad : 
//*  Tenemos un conjunto de variables y le cambiamos su valor, reasignando variables. 
miPrimerVariable = 'esto ha cambiado';
console.log(miPrimerVariable);

// ? Boolean
let miBoolean =  true;
let miOtroBool = false;

// ! Buena práctica: Manejar las primeras letras de las variables con una letra del
// ! abecedario A-Z y en minúscula, debe tener coherencia el nombre de la variable.

let miNumero = 0;
let miNumero2 = 12;
let miNumero3 = -341;

console.log(miNumero, miNumero2, miBoolean, miPrimerVariable);

//todo      Cuando no asignamos a que es igual una variable, como es el caso de acontinuacion,
//todo      luego cuando vayamos a darle al node index.js, en el console log, nos arroja una 
//todo      variable como 'undefined'.

let undef
console.log(undef);

let nulo = null;
console.log('nulo',12, nulo);

